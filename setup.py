from setuptools import find_packages, setup

setup(
    name="common-library-python",
    version="0.0.1",
    description="common library python",
    url="",
    long_description="",
    long_description_content_type="text/markdown",
    author="author_name",
    packages=find_packages(exclude=["tests"]),
    install_requires=[],
)